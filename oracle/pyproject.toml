[project]
name = "check-oracle"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langgraph>=0.3.31",
    "oracledb>=3.1.0",
    "pytest>=8.3.5",
]
